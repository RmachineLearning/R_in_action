
# The commands rely on my file structure and
# so will return an error on your computer

getwd()
setwd("Dropbox/RFunction/code/1202")
list.files()

system("R CMD install packageName")

system("R CMD build packageName")

list.files()
